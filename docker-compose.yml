services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: smart_app
      POSTGRES_PASSWORD: smart_app
      POSTGRES_DB: smart_app
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
